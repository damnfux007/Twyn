package com.twyn.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Twyn color palette.
 * Deep teal primary, warm accents — feels secure and private.
 */
val Primary = Color(0xFF0D9488)           // Teal-600
val PrimaryDark = Color(0xFF0F766E)       // Teal-700
val PrimaryLight = Color(0xFF2DD4BF)      // Teal-400
val Secondary = Color(0xFF6366F1)         // Indigo-500
val SecondaryLight = Color(0xFF818CF8)    // Indigo-400

val Background = Color(0xFF0F172A)        // Slate-900
val Surface = Color(0xFF1E293B)           // Slate-800
val SurfaceLight = Color(0xFF334155)      // Slate-700
val SurfaceVariant = Color(0xFF475569)    // Slate-600

val OnPrimary = Color.White
val OnSecondary = Color.White
val OnBackground = Color(0xFFE2E8F0)     // Slate-200
val OnSurface = Color(0xFFF1F5F9)        // Slate-100
val OnSurfaceVariant = Color(0xFF94A3B8) // Slate-400

// Chat bubble colors
val BubbleSent = Color(0xFF0D9488)        // Teal (sent messages)
val BubbleReceived = Color(0xFF1E293B)    // Dark slate (received messages)
val BubbleSentText = Color.White
val BubbleReceivedText = Color(0xFFE2E8F0)

// Status colors
val OnlineGreen = Color(0xFF22C55E)
val UnreadBadge = Color(0xFFEF4444)
val WarningOrange = Color(0xFFF59E0B)

// Light theme
val LightPrimary = Color(0xFF0D9488)
val LightBackground = Color(0xFFF8FAFC)
val LightSurface = Color.White
val LightOnBackground = Color(0xFF1E293B)
val LightOnSurface = Color(0xFF1E293B)
